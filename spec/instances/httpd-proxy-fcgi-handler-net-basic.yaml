title: Apache httpd mod_proxy_fcgi SetHandler network basic tests

name: httpd-proxy-fcgi-handler-net

description: |
  Set of tests to verify httpd integration when SetHandler directive is used for PHP-FPM for basic scenarios. FPM
  listens on IP address. 

labels: [test, fcgi, net, httpd]

extends:
  name: httpd-proxy-fcgi
  parameters:
    proxy_type: handler
    address_type: net
